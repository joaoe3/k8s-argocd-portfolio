apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: dashboard-app-binding
subjects:
  - kind: ServiceAccount
    name: dashboard-app-sa
    namespace: dashboard-app
roleRef:
  kind: ClusterRole
  name: dashboard-app-role
  apiGroup: rbac.authorization.k8s.io